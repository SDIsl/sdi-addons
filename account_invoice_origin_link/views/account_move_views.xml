<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_move_formview_move_form" model="ir.ui.view">
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <field name="invoice_origin" position="after">
                <field name="related_sale_id" readonly="1" attrs="{'invisible': [('related_sale_id', '=', False)]}"/>
                <field name="related_contract_id" readonly="1" attrs="{'invisible': [('related_contract_id', '=', False)]}"/>
            </field>
        </field>
    </record>
</odoo>
